[phases.install]
cmds = ['composer update --no-dev --optimize-autoloader', 'npm ci', 'npm run build']

[start]
cmd = 'php artisan migrate --force && php-fpm -y /assets/php-fpm.conf -D && nginx -g "daemon off;"'
