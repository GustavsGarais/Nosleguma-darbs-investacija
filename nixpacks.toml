[phases.install]
cmds = ['composer update --no-dev --optimize-autoloader', 'npm ci', 'npm run build']

[start]
cmd = 'cat /assets/nginx.template.conf && mkdir -p /var/log/nginx && php artisan migrate --force && php-fpm -y /assets/php-fpm.conf -D && nginx -g "daemon off;"'
