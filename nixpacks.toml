[phases.install]
cmds = [
  'composer install --no-dev --optimize-autoloader --no-interaction',
  'npm ci',
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'php artisan migrate --force && php-fpm -D && nginx -c /app/nginx.conf -g "daemon off;"'
