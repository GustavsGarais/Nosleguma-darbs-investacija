<div aria-label="{{ __('Password reset from support') }}">
    <a href="{{ route('password.request', ['from' => 'support']) }}" class="btn btn-primary">{{ __('Request password reset email') }}</a>
</div>
